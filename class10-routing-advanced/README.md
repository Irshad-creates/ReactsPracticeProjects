here  is all advanced routes which are used in this 

nested routes - used in product section {can be used for going deep in one routes}

also created 404 pages Route - by using path{*}

useNavigate is used - as navbar2 home button which is like going to any section by using navigate

dynamics routes - is used in navbar2 in (next and back) button which can dynamically change the section as per their funtionality and it also use as history to remember the pages 